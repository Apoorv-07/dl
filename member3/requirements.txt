# Member 3 - minimal dependency set (no training, no model code).
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
scipy>=1.10
PyYAML>=6.0
faiss-cpu>=1.7.4        # HNSW index required by the architecture
fastapi>=0.100          # inference API + analyst view
uvicorn>=0.23           # ASGI server for the above
pytest>=7.4             # tests only

# Deliberately NOT required by Member 3:
#   torch, torch_geometric  -> owned by Member 2 (its adapter may import them itself)
#   any database / queue / container tooling -> out of scope by design
